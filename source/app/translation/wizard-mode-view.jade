div(ng-show='ctrl.getTranslatable()')
  .row
    .col-md-2
      button.form-control(ng-click='ctrl.loadNextModel()') Next
  br
  mas-translation-panel(translatable='ctrl.getTranslatable()')

  mas-save-button(ng-click='ctrl.save()')
